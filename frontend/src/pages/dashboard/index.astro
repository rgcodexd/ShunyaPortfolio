---
import Layout from '../../layouts/Layout.astro';

// Dashboard application shell.
---

<Layout title="Dashboard Login | Shunya Studio" description="Admin dashboard login.">
  <div class="dashboard-shell">
    <div class="login-container" id="loginView">
      <h1 class="text-display">Studio Auth</h1>
      <p class="text-secondary text-body" style="margin-bottom: 24px; text-align: center;">Owner Access Only</p>
      
      <form class="auth-form" id="authForm">
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" required />
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" required />
        </div>
        <button type="submit" class="btn btn-primary w-full" id="signInBtn">Sign In</button>
      </form>
    </div>

    <!-- The actual dashboard that is shown after login -->
    <div class="dashboard-layout hidden" id="dashboardView">
      <aside class="sidebar">
        <h2 class="text-display brand">SHUNYA</h2>
        <nav class="sidebar-nav" id="sidebarNav">
          <button class="nav-tab active" data-target="view-overview">Overview</button>
          <button class="nav-tab" data-target="view-projects">Projects</button>
          <button class="nav-tab" data-target="view-blog">Blog Posts</button>
          <button class="nav-tab" data-target="view-bookings">Bookings</button>
          <button class="nav-tab" data-target="view-comments">Comments</button>
          <button class="nav-tab" data-target="view-media">Media Library</button>
          <button class="nav-tab" data-target="view-settings">Settings</button>
        </nav>
        <button class="btn btn-outline w-full" id="signOutBtn" style="margin-top: auto;">Sign Out</button>
      </aside>
      
      <main class="dashboard-main">
        <header class="dashboard-header">
          <h2 class="text-display" id="dashboardTitle">Overview</h2>
          <div class="user-profile" id="userEmailBadge">Loading...</div>
        </header>
        
        <div class="dashboard-content">
          
          <!-- Overview Tab -->
          <div id="view-overview" class="tab-content active">
            <div class="stats-grid">
              <div class="stat-card">
                <h3>Active Projects</h3>
                <p class="text-display stat-number">12</p>
              </div>
              <div class="stat-card">
                <h3>New Bookings</h3>
                <p class="text-display stat-number">3</p>
              </div>
              <div class="stat-card">
                <h3>Total Views</h3>
                <p class="text-display stat-number">45.2k</p>
              </div>
            </div>
            
            <div class="recent-activity">
              <h3 class="text-display" style="margin-bottom: 16px;">Recent Activity</h3>
              <div class="activity-list">
                <div class="activity-item">New booking request from "Epic Games"</div>
                <div class="activity-item">Comment on "Optimizing Nanite for Foliage"</div>
                <div class="activity-item">Project "Cyberpunk Alley" reached 1,000 views</div>
              </div>
            </div>
          </div>

          <!-- Projects Tab -->
          <div id="view-projects" class="tab-content hidden">
            <h3 class="text-display">Manage Projects</h3>
            <p class="text-secondary">Here you will be able to add, edit, and delete your portfolio projects.</p>
            <button class="btn btn-primary" style="margin-top: 1rem;" onclick="alert('Creating project form coming soon!')">Create New Project</button>
          </div>

          <!-- Blog Tab -->
          <div id="view-blog" class="tab-content hidden">
            <h3 class="text-display">Manage Blog Posts</h3>
            <p class="text-secondary">Draft and publish articles here.</p>
            <button class="btn btn-primary" style="margin-top: 1rem;" onclick="alert('Blog editor coming soon!')">Write New Post</button>
          </div>

          <!-- Bookings Tab -->
          <div id="view-bookings" class="tab-content hidden">
            <h3 class="text-display">Incoming Bookings</h3>
            <p class="text-secondary">View all contact and project requests.</p>
            <div class="activity-list" style="margin-top: 1rem;">
              <div class="activity-item"><strong>John Doe</strong> - Wants to hire you for a 3D animation project.</div>
            </div>
          </div>

          <!-- Comments Tab -->
          <div id="view-comments" class="tab-content hidden">
            <h3 class="text-display">Comment Moderation</h3>
            <p class="text-secondary">Approve or delete user comments.</p>
          </div>

          <!-- Media Tab -->
          <div id="view-media" class="tab-content hidden">
            <h3 class="text-display">Media Library</h3>
            <p class="text-secondary">Upload images and digital products securely to your Supabase buckets.</p>
          </div>

          <!-- Settings Tab -->
          <div id="view-settings" class="tab-content hidden">
            <h3 class="text-display">Admin Settings</h3>
            <p class="text-secondary">Manage your studio configuration and passwords.</p>
          </div>

        </div>
      </main>
    </div>
  </div>
</Layout>

<style>
  .dashboard-shell {
    min-height: 100vh;
    background: var(--bg-color);
  }
  
  .login-container {
    max-width: 400px;
    margin: 15vh auto;
    padding: var(--spacing-8);
    background: var(--surface-color);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
  }
  
  .login-container h1 {
    text-align: center;
    margin-bottom: var(--spacing-2);
  }
  
  .auth-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
  }
  
  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
  }
  
  label {
    font-size: 0.85rem;
    font-weight: 600;
  }
  
  input {
    background: var(--surface-elevated);
    border: 1px solid var(--border-color);
    color: var(--text-primary);
    padding: var(--spacing-2);
    border-radius: var(--radius-sm);
  }
  
  .w-full {
    width: 100%;
  }
  
  .btn {
    padding: var(--spacing-2);
    font-weight: 600;
    border-radius: var(--radius-sm);
    cursor: pointer;
  }
  
  .btn-primary {
    background: var(--accent-color);
    color: var(--bg-color);
    border: none;
  }
  
  .btn-outline {
    background: transparent;
    border: 1px solid var(--border-color);
    color: var(--text-primary);
  }
  
  /* Dashboard Layout */
  .dashboard-layout {
    display: flex;
    height: 100vh;
    overflow: hidden;
  }
  
  .sidebar {
    width: 250px;
    background: var(--surface-color);
    border-right: 1px solid var(--border-color);
    padding: var(--spacing-6);
    display: flex;
    flex-direction: column;
  }
  
  .brand {
    margin-bottom: var(--spacing-8);
    color: var(--accent-color);
  }
  
  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2);
  }
  
  .nav-tab {
    color: var(--text-secondary);
    padding: var(--spacing-2);
    border-radius: var(--radius-sm);
    font-weight: 500;
    background: transparent;
    border: none;
    text-align: left;
    cursor: pointer;
    font-family: inherit;
    font-size: 1rem;
  }
  
  .nav-tab:hover, .nav-tab.active {
    background: var(--surface-elevated);
    color: var(--text-primary);
  }
  
  .dashboard-main {
    flex-grow: 1;
    overflow-y: auto;
    padding: var(--spacing-8);
  }
  
  .dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-8);
    padding-bottom: var(--spacing-4);
    border-bottom: 1px solid var(--border-color);
  }
  
  .user-profile {
    background: var(--surface-color);
    padding: var(--spacing-1) var(--spacing-3);
    border-radius: 50px;
    border: 1px solid var(--border-color);
    font-weight: 600;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-6);
    margin-bottom: var(--spacing-8);
  }
  
  .stat-card {
    background: var(--surface-color);
    padding: var(--spacing-6);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
  }
  
  .stat-card h3 {
    color: var(--text-secondary);
    font-size: 0.9rem;
    text-transform: uppercase;
    margin-bottom: var(--spacing-2);
  }
  
  .stat-number {
    font-size: 3rem;
    color: var(--accent-color);
    margin: 0;
  }
  
  .activity-list {
    background: var(--surface-color);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
    overflow: hidden;
  }
  
  .activity-item {
    padding: var(--spacing-4);
    border-bottom: 1px solid var(--border-color);
  }
  
  .activity-item:last-child {
    border-bottom: none;
  }
  
  .hidden {
    display: none !important;
  }
</style>

<script>
  import { supabase } from '../../lib/supabase';

  const loginView = document.getElementById('loginView');
  const dashboardView = document.getElementById('dashboardView');
  const authForm = document.getElementById('authForm');
  const signInBtn = document.getElementById('signInBtn');
  const signOutBtn = document.getElementById('signOutBtn');
  const userEmailBadge = document.getElementById('userEmailBadge');
  
  // Tab Elements
  const navTabs = document.querySelectorAll('.nav-tab');
  const tabContents = document.querySelectorAll('.tab-content');
  const dashboardTitle = document.getElementById('dashboardTitle');

  // Check if already logged in on load
  async function checkAuth() {
    const { data: { session } } = await supabase.auth.getSession();
    if (session) {
      showDashboard(session.user);
    }
  }

  function showDashboard(user) {
    if (loginView && dashboardView) {
      loginView.classList.add('hidden');
      dashboardView.classList.remove('hidden');
      
      if (userEmailBadge) {
        userEmailBadge.textContent = user.email;
      }
      
      // Hide global header/footer for dashboard
      const header = document.querySelector('.header');
      const footer = document.querySelector('.footer');
      if (header) header.setAttribute('style', 'display: none;');
      if (footer) footer.setAttribute('style', 'display: none;');
    }
  }

  function hideDashboard() {
    if (loginView && dashboardView) {
      loginView.classList.remove('hidden');
      dashboardView.classList.add('hidden');
      const header = document.querySelector('.header');
      const footer = document.querySelector('.footer');
      if (header) header.removeAttribute('style');
      if (footer) footer.removeAttribute('style');
    }
  }

  // Handle Login
  if (authForm) {
    authForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      const email = (document.getElementById('email') as HTMLInputElement).value;
      const password = (document.getElementById('password') as HTMLInputElement).value;
      
      if (signInBtn) signInBtn.textContent = "Authenticating...";
      
      const { data, error } = await supabase.auth.signInWithPassword({
        email,
        password
      });

      if (error) {
        alert("Login failed: " + error.message);
        if (signInBtn) signInBtn.textContent = "Sign In";
      } else {
        if (signInBtn) signInBtn.textContent = "Sign In";
        showDashboard(data.user);
      }
    });
  }
  
  // Handle Sign Out
  if (signOutBtn) {
    signOutBtn.addEventListener('click', async () => {
      const { error } = await supabase.auth.signOut();
      if (error) {
        alert("Error signing out.");
      } else {
        hideDashboard();
      }
    });
  }

  // Handle Tab Switching
  navTabs.forEach(tab => {
    tab.addEventListener('click', () => {
      // Remove active class from all tabs
      navTabs.forEach(t => t.classList.remove('active'));
      // Add active class to clicked tab
      tab.classList.add('active');
      
      // Hide all contents
      tabContents.forEach(content => content.classList.add('hidden'));
      
      // Show targeted content
      const targetId = tab.getAttribute('data-target');
      if (targetId) {
        const targetElement = document.getElementById(targetId);
        if (targetElement) {
          targetElement.classList.remove('hidden');
        }
      }
      
      // Update Title
      if (dashboardTitle) {
        dashboardTitle.textContent = tab.textContent;
      }
    });
  });

  // Run on load
  checkAuth();
</script>
